# Agriculture ML API — Python Dependencies
# Model 5 (Plant Disease) + Model 2 (Crop Recommendation) on one server

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9     # required for UploadFile / form-data

# Pydantic (FastAPI uses v2)
pydantic>=2.0.0

# Model 5 — EfficientNet-B0 inference
torch>=2.0.0
torchvision>=0.15.0
Pillow>=10.0.0               # image decoding and preprocessing

# Model 2 — Crop recommendation engine
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.7.0          # 1.6.1 has no wheel for Python 3.13+; newer sklearn loads the pkl with a harmless version warning
joblib>=1.3.0

# Utilities
python-dotenv>=1.0.0         # .env loading
