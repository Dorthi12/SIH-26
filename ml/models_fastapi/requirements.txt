# Unified Agriculture ML API — Python Dependencies
# Model 1 (CatBoost Yield Forecaster) + Model 2 (Crop Recommendation)
# Model 3 V3 (Zero-Production Risk) + Model 5 (Plant Disease)

# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9     # required for UploadFile / form-data

# ── Pydantic (FastAPI uses v2) ─────────────────────────────────────────────
pydantic>=2.0.0

# ── Model 5 — EfficientNet-B0 Plant Disease Detection ─────────────────────
torch>=2.0.0
torchvision>=0.15.0
Pillow>=10.0.0              # image decoding and preprocessing

# ── Model 2 — Crop Recommendation Engine ──────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# ── Model 1 — CatBoost Future Yield Forecaster ────────────────────────────
catboost>=1.2.0             # also used by Model 3 V3

# ── Model 3 V3 — Zero-Production Risk (CatBoost + Isotonic Calibrator) ────
# IMPORTANT: the isotonic calibrator (.joblib) was serialized with
# scikit-learn 1.6.1. Pinning to that exact version avoids the
# InconsistentVersionWarning and guarantees identical calibration output.
# If you re-export the calibrator under a different sklearn version,
# update this pin to match.
scikit-learn==1.6.1

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # .env loading
httpx>=0.27.0               # for downloading images from S3 URLs
